# `graphnetz.models`

The five benchmark architectures, the DGI pre-training utility, and the
adapters that let a node-level encoder serve every task family. See
[Models](../models/index.md) for the narrative version, one page per
architecture.

::: graphnetz.models

## Task type adapters

::: graphnetz.models._adapters
    options:
      members:
        - GraphLevelWrapper
        - LinkPredWrapper
        - DGIWrapper
